name: Test

on:
  workflow_dispatch:

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Test
              uses: ./test-local-action
              with:
                  num1: 1
